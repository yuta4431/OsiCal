<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>カテゴリー詳細</title>
    <link rel="stylesheet" href="/OshiCal/public/assets/css/style.css">
</head>
<body>
    {% if category %}
        <h1>{{ category.name }}</h1>

        {% if template and template.template %}
            <div style="margin: 20px 0;">
                <h3>メモテンプレート</h3>
                <pre style="background: #f5f5f5; padding: 10px; border-radius: 4px;">{{ template.template }}</pre>
            </div>
        {% endif %}

        <div>
            <button type="button" onclick="location.href='/OshiCal/public/index.php?route=category/edit&id={{ category.id }}'">編集</button>
            <button type="button" onclick="if(confirm('本当に削除しますか？')) location.href='/OshiCal/public/index.php?route=category/destroy&id={{ category.id }}'" style="margin-left: 10px;">削除</button>
        </div>
    {% else %}
        <p>カテゴリーが見つかりません。</p>
    {% endif %}

    <div style="margin-top: 20px;">
        <button type="button" onclick="location.href='/OshiCal/public/index.php?route=category/index'">戻る</button>
    </div>
</body>
</html>
